<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.service.core.comment.repository.mapper.CommentMapper">
    <select id="findCommentDtoListByPaging" parameterType="com.service.core.comment.dto.CommentSearchDto"
            resultType="com.service.core.comment.dto.CommentDto">
        select comment_id,
               comment,
               comment_image,
               parent_id,
               date_format(register_time, '%y.%m.%d %H:%i') as register_time,
               user_id,
               user_password,
               user_profile_image,
               user_nickname,
               target_user_id,
               target_user_nickname,
               is_owner,
               is_delete,
               secret,
               anonymous
        from comment
        where post_id = #{postId} LIMIT #{commentSearchPagingDto.commentPagination.limitStart}
            , #{commentSearchPagingDto.recordSize}
    </select>

    <select id="findCommentCount" resultType="int">
        SELECT count(*)
        from comment
        where post_id = #{postId}
    </select>

    <select id="findCommentCountExist" resultType="int">
        SELECT count(*)
        from comment
        where post_id = #{postId}
          and is_delete = false;
    </select>

    <select id="findChildCommentCount" resultType="int">
        SELECT count(*)
        from comment
        where parent_id = #{commentId};
    </select>
</mapper>